/*
    Las expresiones regulares son un mecanismo para encontrar patrones en cadenas de texto.
    Permite encontrar de una maneter muy simple los textos que se necesitan, ya que estos
    requieren cierta "sofisticación" para ser encontrados, cuestión que no siempre lo permite
    un algoritmo sencillo.
*/
var expresion1 = new RegExp('abc');

var expresion2 = /abc/;

var parrafo = /text1 \/n texto2 /;